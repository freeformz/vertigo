<h1>Settings</h1>
<form method="post" action="/user/settings">
	<fieldset>

		<label>Allow new registrations</label>
		<p>Below you can choose whether you'd like disable registration for new users.</p>
		<input type="radio" name="allowregistrations" value="true"{{ if eq .Data.Settings.AllowRegistrations true }} checked{{ end }}> Allow registrations
		<br>
		<input type="radio" name="allowregistrations" value="false"{{ if eq .Data.Settings.AllowRegistrations false }} checked{{ end }}> Disallow registrations

		<br><br>

		<label>Hostname</label>
		<p>The URL used to generate RSS and Atom links and any emails that link back to your site. This should be the absolute URL. Please include http:// or https:// and leave off any trailing forward slashes "/"</p>
		<input name="hostname" placeholder="http://example.com" required="required" value="{{ .Data.Settings.Hostname }}">

		<br><br>

		<label>Blog name</label>
		<p>This is the text people see on their browser tabs when visiting your homepage.</p>
		<input name="name" placeholder="Foo's Blog" required="required" value="{{ .Data.Settings.Name }}">

		<br><br>

		<label>Blog description</label>
		<p>Your beloved site's description. Used in RSS and Atom feeds.</p>
		<input name="description" placeholder="Thoughts about which witch is which" required="required" value="{{ .Data.Settings.Description }}">

		<br><br>

		<label>Timezone</label>
		<p>Your timezone</p>
		<select name="timezone">
		{{range $abbrs := .Data.Abbrs}}
			<option value="{{$abbrs.LOC}}"{{if TimeZoneEQ .LOC}} selected{{end}}>{{$abbrs.LOC}}</option>
		{{end}}
		</select>

		<br><br>

		<h3>SMTP settings</h3>
		<p>Vertigo can use SMTP to send out password reminders. Though if you think there's no chance you could lose your password, then you don't have to fill these. You can also set these on a later date, provided that you can login to the site or have access to the server root folder.</p>	

		<label>SMTP Login</label>
		<p>Address from which you want to send mail from.</p>
		<input name="login" placeholder="postmaster@example.com" value="{{ .Data.Settings.Mailer.Login }}">		

		<br><br>

		<label>SMTP Password</label>
		<p>Password for the address you gave above.</p>
		<input name="password" value="{{ .Data.Settings.Mailer.Password }}">

		<br><br>

		<label>SMTP Port</label>
		<p>On some occasions this might be 25 instead.</p>
		<input name="port" value="587" value="{{ .Data.Settings.Mailer.Port }}">

		<br><br>

		<label>SMTP Hostname</label>
		<p>SMTP server address.</p>
		<input name="smtp-hostname" placeholder="smtp.example.org" value="{{ .Data.Settings.Mailer.Hostname }}">

		<br><br>

		<button type="submit">Save</button>

	</fieldset>
</form>
